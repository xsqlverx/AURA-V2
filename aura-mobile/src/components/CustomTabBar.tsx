// CustomTabBar — deprecated since pivot to Drawer navigation.
// Kept to avoid broken imports during transition.
// Remove this file once all references are confirmed dead.
export default function CustomTabBar() {
  return null;
}
