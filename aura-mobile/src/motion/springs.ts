import { spring as springTokens } from '../tokens/animation';

export const SpringConfig = {
  default: springTokens.default,
  gentle: springTokens.gentle,
  snappy: springTokens.snappy,
  toggle: springTokens.toggle,
} as const;
